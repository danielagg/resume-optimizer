# React + TypeScript + Vite + shadcn/ui

Template: Vite + React + TypeScript + shadcn/ui.

## Adding components

Add component:

```bash
npx shadcn@latest add button
```

Places ui components in `src/components`.

## Using components

Import:

```tsx
import { Button } from "@/components/ui/button"
```